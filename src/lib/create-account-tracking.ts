/** Passed as `source` on `create_account_clicked` for funnels and LD metrics. */
export type CreateAccountClickSource =
  | "header"
  | "hero"
  | "login_prompt"
  | "account_anonymous"
  | "paywall";
